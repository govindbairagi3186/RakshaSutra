export async function loadProfile(): Promise<{ name?: string } | null> {
  // Minimal stub: return null so onboarding shows by default.
  // Replace with real storage retrieval (SecureStore/AsyncStorage) in your app.
  return null;
}
